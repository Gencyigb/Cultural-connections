extends layouts/layout.pug

block content
  h2 Chat Page

  p You are chatting with user ID: #{otherUserId}

  .chat-box
    p No messages yet...

  form(method="POST")
    input(type="text", name="message", placeholder="Type a message...", required)
    button(type="submit") Send

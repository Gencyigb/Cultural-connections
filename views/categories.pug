extends layouts/layout.pug

block content
  h1 Categories

  // Display grouped categories from database
  each category in categories
    .category-card
      h2= category.name
      p Posts: #{category.count}
